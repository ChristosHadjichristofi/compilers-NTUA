let main = 
	if (1.3 = 1.3) then print_string "right!\n" else print_string "wrong!\n";
	if (12.1 <> 12.1) then print_string "wrong!\n" else print_string "right!\n";
	if (1.3 < 2.3) then print_string "right!\n" else print_string "wrong!\n";
	if (0.1 > 1.3) then print_string "wrong!\n" else print_string "right!\n";
	if (1.2 <= 1.3) then print_string "right!\n" else print_string "wrong!\n";
	if (5.0 >= 1.3) then print_string "right!\n" else print_string "wrong!\n"