let rec even x =
	if (x=0) then true else (odd (x-1))
and odd x =
	if (x=0) then false else (even (x-1))

let main =
    let a = even 10 in
    if (a = true) then print_int 10 else print_int 0

(*works*)